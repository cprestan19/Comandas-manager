# Copiar a .env.local y rellenar valores reales

DATABASE_URL="postgresql://usuario:password@host:5432/comandas_manager"

NEXTAUTH_SECRET="genera con: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"

# Pusher (crear cuenta gratis en pusher.com → Channels → Create app)
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER="us2"

NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="us2"

# Protege el endpoint /api/seed
SEED_SECRET="secreto-largo-aleatorio"
